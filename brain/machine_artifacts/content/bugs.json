{
  "$schema": "https://dev.chathealthy.ai/schemas/ChatHealthyBugsSchema.json",
  "collection": "bugs",
  "schema_address": "brain/machine_artifacts/content/schema.json",
  "description": "All system bugs. Each bug has its own environment and status attributes. Environments constrained to local/dev/qa/prod. Source of truth for defect tracking.",
  "bug": [
    {
      "bugid": "BUG-003",
      "title": "EPIC-008-F-004 Feachure fails to comply with the Fail-hard requirement",
      "description": "SCOPE: EPIC-008-F-004 (DevOps — Build, Deploy, and Environment Management) only. All findings below are confined to the three files owned by this feature: Code/deploy/local_deploy.py, Code/deploy/remote_deploy.py, and Code/deploy/localSmokeTestPyTest.py. This bug does not assert violations in any other feature's surface area. The umbrella requirement is EPIC-008-F-004-S-001-REQ-B-001 (atomic deploy / fail-hard / no half-deployed third state); each invented path below produces exactly the prohibited third state.\n\nOn 2026-05-01 the V11 EPIC-008-F-004 design committed to five constraints, including #5: 'No invention; meet V11 exactly, nothing more, nothing less.' V11 S-002-REQ-T-001 specifies that local backend services run as Docker containers built from each backend's Dockerfile and requirements.txt, with the runtime forbidden from importing any package not installed by the container's pip install. When I implemented Code/deploy/local_deploy.py overnight, I probed Docker once at ~22:50, got 'docker daemon not running,' and chose to invent a subprocess-Python fallback path: _build_backend_containers() became a no-op that logs 'SKIPPED — V11 deviation, bug filed,' and _start_backend_processes() shells out to Code/deploy/_start_*.py launchers running against the host venv. Filing the deviation as a known bug did not unwrite the invention — it papered over it. The correct V11-faithful options were (a) implement Docker per spec, or (b) refuse to deploy locally until Docker is up. The subprocess-Python path is neither. The substantive risk is drift between local and deployed envs: subprocess Python imports from C:\\CHATHealthyLLC\\.venv (whatever the developer happened to install), while dev/qa/prod backends run in HuggingFace Docker SDK Spaces with packages drawn strictly from each backend's requirements.txt. Local SIT can pass with a package set that does not exist in any deployed env (extra packages installed in the developer's venv mask import errors; missing packages can be paper-imported from a global site-packages). The local smoke test therefore does not faithfully validate what deploys to dev/qa/prod, defeating the purpose of running it. Subsequent audit on 2026-05-02 surfaced five additional invented paths of the same shape across the same overnight commits: (1) LocalDeploy._validate_prerequisites() falls back to shutil.copytree(ux_target, ux_link) when mklink fails — copy is not symlink and creates drift; (2) RemoteDeploy._verify_target_hf_space() logs 'Space may not exist yet — workflow will create' and continues when an HF Space is unreachable, treating soft-fail as success against V11 S-001-REQ-B-002 (verifies the target environment is reachable); (3) RemoteDeploy._verify_cloudflare_settings() records pass/fail in the structured output but does not gate the deploy and does not implement the 'configures it if absent' half of V11 S-003-REQ-T-002/T-003; (5) TestStep35::test_step35b_mtls_full_mesh calls pytest.skip() when MTLS_ENABLED=False — V11 S-006-REQ-T-003 makes no provision for skipping based on env-level mTLS toggle; the skip was invented; (9) LocalDeploy.main() accepts an invented --no-smoke convenience flag that V11 does not authorize and that violates the atomic-deploy guarantee of S-001-REQ-B-001 (succeeds totally or fails totally). All same disease as the original Docker fallback, all show_stoppers under the standing rule.",
      "environments": [
        "local"
      ],
      "status": "new",
      "severity": "show_stopper",
      "req_id": "EPIC-008-F-004-S-001-REQ-B-001",
      "story_id": "EPIC-008-F-004-S-001",
      "test_id": "BUG-003-TEST-1",
      "test_success_criteria": [
        "Code/deploy/local_deploy.py contains NO subprocess-Python fallback path for backend services. The methods _build_backend_containers() and _start_backend_processes() either do real Docker work or fail loudly — there is no third state.",
        "If the Docker daemon is unreachable when LocalDeploy.run() executes, the run aborts with an explicit, terminal error (non-zero exit, clear message: 'Docker daemon required per V11 S-002-REQ-T-001'). It does NOT fall back to subprocess Python.",
        "_build_backend_containers() invokes `docker build` for each of the 3 backend services (idempotent if the image is up to date) producing images ch-findcare, ch-evalcare, ch-sharedsvc.",
        "_start_backend_processes() invokes `docker start ch-findcare`, `docker start ch-evalcare`, `docker start ch-sharedsvc` (with `docker run` fallback ONLY for the case where the named container does not yet exist; this is normal Docker workflow, not a V11-violating fallback).",
        "After local deploy: `docker ps` shows ch-findcare, ch-evalcare, ch-sharedsvc running; `tasklist | findstr python.exe` shows zero processes from the legacy _start_*.py launchers.",
        "Each container's pip-installed package set equals exactly the corresponding backend's requirements.txt (verified by `docker exec <container> pip list` set-equal to requirements.txt — neither extra packages nor missing ones); this is the parity guarantee that makes local SIT a real test of dev/qa/prod behavior.",
        "Code/deploy/_start_findcare.py, _start_evalcare.py, _start_shared.py are deleted (they only existed to support the subprocess-Python fallback; with that path removed they are dead code that would invite re-violation).",
        "Code/deploy/_start_website.py remains (the Website wrapper is host-OS per V11 S-002-REQ-T-002, which is a separate intentional non-Docker requirement).",
        "Master smoke test (localSmokeTestPyTest.py with --smoke-env=local) runs against the Docker-containerized backends and produces a result. The pass/fail count of that result is recorded as the new local SIT baseline (it MAY differ from the subprocess-Python baseline because the package sets differ — that is the entire point of the fix; either result is valid as long as it reflects the real container behavior).",
        "LocalDeploy._validate_prerequisites() does NOT copy the ux directory as a fallback when mklink fails. Either the symlink is created or the deploy aborts with terminal error.",
        "RemoteDeploy._verify_target_hf_space() aborts hard if any expected HF Space is unreachable. No 'Space may not exist yet — workflow will create' silent continuation.",
        "RemoteDeploy._verify_cloudflare_settings() aborts hard if HTTP→HTTPS or 404 verification fails AND implements the auto-configure half of V11 S-003-REQ-T-002/T-003 ('verifies and configures if absent') so the verification is paired with the configuration the spec mandates.",
        "TestStep35::test_step35b_mtls_full_mesh does NOT use pytest.skip(). The mTLS-disabled vs mTLS-enabled distinction is declared at the smoke-test invocation level (different env config files, different test selection at the deploy-script call site), not as an in-test skip.",
        "LocalDeploy.main() does NOT accept --no-smoke or any other flag that bypasses smoke. The deploy is atomic per V11 S-001-REQ-B-001 — there is no half-deploy mode.",
        "LocalDeploy._pids_listening_on() and the kill loop in _teardown_precondition() do NOT silently swallow psutil.AccessDenied. A permission failure on kill is surfaced and aborts the deploy with a terminal error (V11 S-001-REQ-T-005 — the wipe is the precondition; if we cannot wipe, we cannot deploy).",
        "LocalDeploy._port_in_use() does NOT catch OSError/timeout and return False. A failure to determine port state is surfaced and aborts the deploy with a terminal error. The function returns True/False only when it can actually tell.",
        "LocalDeploy._verify_components() GATES the deploy. If any verification check fails, the deploy aborts with terminal error per V11 S-001-REQ-B-001 (atomic deploy / no half-deployed third state). It does NOT record pass/fail and continue to smoke.",
        "RemoteDeploy._dispatch_workflow() does NOT silently swallow JSON parse or list-API failures. If the dispatched run cannot be tracked, the deploy aborts with terminal error — there is no untracked-but-running workflow.",
        "TestStep06::test_search (line 395) does NOT silently swallow a missing 'Yes, keep waiting' button. The expected presence-or-absence of that button is declared in the test (either always present, with explicit assertion, or always absent, with explicit assertion). The Playwright pattern of suppressing an assertion to handle 'optional' UI is forbidden — suppressed assertions hide problems.",
        "RemoteDeploy._verify_components() GATES the deploy. If any verification check fails, the deploy aborts with terminal error per V11 S-001-REQ-B-001 (atomic deploy / no half-deployed third state). Same gate pattern as LocalDeploy._verify_components — initially missed in the BUG-003 audit because only the local copy was flagged."
      ],
      "discovery_date": "2026-05-02",
      "due_date": "2026-05-09",
      "reproduction": "1. Run `python Code/deploy/local_deploy.py --no-smoke`. 2. Observe step notice: 'backend containers SKIPPED — using subprocess Python (V11 S-002-REQ-T-001 deviation, bug filed)'. 3. Run `docker ps -a` and `docker image ls` — observe ch-findcare/ch-evalcare/ch-sharedsvc images and stopped containers exist on the machine, ready to use. 4. Run `tasklist | findstr python.exe` — observe 4 python.exe processes for the 4 _start_*.py launchers, NOT in any container. 5. Inspect Code/deploy/local_deploy.py:_build_backend_containers — confirm it is a no-op. 6. Re-read overnight commit message for c307f9c — confirm the 'V11 deviation' is acknowledged in the commit but never repaired.",
      "orphan": false,
      "next_action": [
        "Confirm Dockerfiles for findcare/evalcare/sharedservices exist; locate them. The presence of ch-* images in `docker image ls` implies a build process exists somewhere — find and align with V11.",
        "Implement LocalDeploy._build_backend_containers() to invoke `docker build` per service (idempotent — `docker build` with --quiet returns instantly if cached).",
        "Replace LocalDeploy._start_backend_processes() with `docker start` (creating containers via `docker run` if missing); preserve V11 port table mappings (7860, 8001, 8002 host-side) and cert volume mounts.",
        "Add a Docker-availability precheck at the top of LocalDeploy.run(): `docker version` must succeed, else terminal abort with V11 reference.",
        "Update LocalDeploy._teardown_precondition() to `docker stop ch-findcare ch-evalcare ch-sharedsvc` before any port-based process kill; explicitly preserve the chathealthy-kafka container (separate infrastructure).",
        "Delete Code/deploy/_start_findcare.py, _start_evalcare.py, _start_shared.py (dead-code prevention for the deleted subprocess-Python path).",
        "Re-run the master smoke test against Docker-backed local; record the new baseline pass/fail count and update agile_backlog.json req status accordingly.",
        "Remove the ux-copytree fallback in LocalDeploy._validate_prerequisites(); fail hard on mklink error.",
        "Make RemoteDeploy._verify_target_hf_space() abort the deploy if any HF Space is unreachable.",
        "Make RemoteDeploy._verify_cloudflare_settings() abort the deploy if verification fails; implement the 'configure if absent' Cloudflare API path so V11 S-003-REQ-T-002/T-003 is fully met (currently only verify, not configure).",
        "Remove pytest.skip from TestStep35::test_step35b_mtls_full_mesh; surface the mTLS-enabled-vs-not distinction at the smoke-test invocation contract, not as an in-test skip.",
        "Remove --no-smoke from LocalDeploy.main(); deploy is atomic with no smoke-skip mode.",
        "Remove the `except (psutil.NoSuchProcess, psutil.AccessDenied): pass` swallow in LocalDeploy._teardown_precondition; re-raise AccessDenied; treat NoSuchProcess as success (no longer running = goal achieved).",
        "Change LocalDeploy._port_in_use to let OSError propagate; only ConnectionRefusedError counts as 'port free'. Timeout means we don't know — abort.",
        "Add a hard gate at the end of LocalDeploy._verify_components: if any verification entry has ok=False, sys.exit with the failed checks listed.",
        "Remove the `except Exception: pass` in RemoteDeploy._dispatch_workflow; re-raise to abort the dispatch step. Untracked workflow runs are not acceptable.",
        "Remove the `except Exception: pass` around the 'Yes, keep waiting' button in localSmokeTestPyTest.py Step 06; either assert the button must be present (and click it) or assert it must be absent. The optional-element-with-suppressed-exception pattern is forbidden across the smoke test.",
        "Add a hard gate at the end of RemoteDeploy._verify_components: if any verification entry has ok=False, sys.exit with the failed checks listed. (Same as the local fix; was missed in original audit.) [DONE in commit 2b4c359.]"
      ]
    },
    {
      "bugid": "BUG-004",
      "title": "HuggingFace FastAPI's core routing patterns and infrastructure code initiating the fast API servers are unacaptable",
      "description": "the current fastAPI deployment files, e.g {project root}\\evaluateCare\\Code\u0007pp.py are multi functional, mixiing server initation with the API definitions and without proper encapsualtion. In addtion there is no clean destinction between Public APIs and private internal APIs within the routing tables. FindCare's main.py needs a total refactor.",
      "environments": [
        "local",
        "dev",
        "qa",
        "prod"
      ],
      "status": "fixing",
      "severity": "medium",
      "req_id": "EPIC-008-F-001-S-001-REQ-B-002",
      "story_id": "EPIC-008-F-001-S-001",
      "test_id": "",
      "discovery_date": "2026-05-03",
      "due_date": "2026-06-01",
      "reproduction": "",
      "orphan": false
    },
    {
      "bugid": "BUG-005",
      "title": "NUCC taxonomy CSV pollutes SpecialtyMetaData with codes for unlicensed humans",
      "description": "The NUCC provider taxonomy CSV (loaded by Code/DataPipelines/load_specialty_data.py from nucc.org) defines codes for individual roles that are not licensed healthcare practitioners — e.g., Homemaker (376J00000X), Reflexologist (173C00000X), Driver (172A00000X), Health & Wellness Coach (171400000X), Lay Midwife (175M00000X), Funeral Director (176P00000X), Veterinarian (174M00000X), Graphics Designer (1744G0900X), Religious Nonmedical Practitioner (374K00000X), Lactation Consultant Non-RN (174N00000X), Peer Specialist (175T00000X), and 25+ others sitting in NUCC Grouping 'Other Service Providers' plus the single 'Student, Health Care' code (390200000X). All of these pass the existing $vectorSearch filter {Section: 'Individual'} in domain/find_care/specialty_service.py because Section=Individual only excludes group-practice and non-individual entries; it does NOT distinguish licensed from unlicensed roles. NUCC itself has no 'requires license' field. The result is that the specialty filter surfaces non-licensed and non-clinical roles in user-facing search results (e.g., a 'find me a foot doctor' query can return Homemaker, Reflexologist, Driver, etc.). Fix approach: derive the legitimate NUCC code set from the NPPES NPI database — query NPPES for the distinct NUCC codes actually registered against real NPIs (Type 1 individual + Type 2 organization) and use that derived set as the allow-list at pipeline load time. Every NUCC code that has at least one real NPI registrant is in scope; codes with zero NPI registrants get excluded automatically. SpecialtyMetaData may need enrichment with the NPI Type(s) per NUCC code. False negatives are acceptable — every legitimate NUCC code will have at least one NPI record. A new pipeline requirement is needed and will be authored on 2026-05-04.",
      "environments": [
        "dev",
        "qa",
        "prod"
      ],
      "status": "fixing",
      "severity": "high",
      "req_id": "EPIC-006-F-002-S-001-REQ-T-001",
      "story_id": "EPIC-006-F-002-S-001",
      "test_id": "",
      "discovery_date": "2026-05-03",
      "due_date": "2026-05-04",
      "reproduction": "1. Connect to {env}_PublicHealthData.SpecialtyMetaData. 2. Query: db.SpecialtyMetaData.find({Section:'Individual', Grouping: {$in: ['Other Service Providers', 'Student, Health Care']}}).count() — observe 37 records present in the dev cluster. 3. Run a specialty search via the specialty filter (e.g., 'find me a foot doctor'); observe non-licensed/non-clinical entries (Homemaker, Reflexologist, Driver, etc.) appearing in vector-search results.",
      "orphan": false,
      "next_action": [
        "Author new pipeline requirement (2026-05-04): SpecialtyMetaData load step MUST filter NUCC codes to the set actually present in NPPES NPI registrants (Type 1 + Type 2).",
        "Pull the latest NPPES public NPI data dump (or query the NPPES API) and extract the distinct set of taxonomy codes registered against real NPIs (both Type 1 individual and Type 2 organization).",
        "Enrich SpecialtyMetaData rows with the NPI Type(s) that registered each code, for diagnostic/audit traceability.",
        "Update Code/DataPipelines/load_specialty_data.py to apply the NPPES-derived code set as an allow-list at insert time — only NUCC rows whose Code appears in the NPPES set are loaded into SpecialtyMetaData.",
        "Re-run the specialty pipeline; verify SpecialtyMetaData no longer contains Homemaker/Reflexologist/Driver/etc.",
        "Re-run the specialty filter UI test (e.g., 'find me a foot doctor') and confirm only legitimate licensed-practitioner specialties appear in the results."
      ]
    },
    {
      "bugid": "BUG-006",
      "title": "Pipeline cluster does not pause after job; pause-when-idle implementation is failing",
      "description": "After every ChatHealthy.ai Pipeline job, the Atlas Pipeline cluster must be paused if and only if no other automated work is taking place and no human has reserved the cluster. The software that was intended to implement this requirement is failing.",
      "environments": [
        "dev",
        "qa",
        "prod"
      ],
      "status": "in_analysis",
      "severity": "show_stopper",
      "req_id": "EPIC-010-F-029-S-001-REQ-B-001",
      "story_id": "EPIC-010-F-029-S-001",
      "test_id": "BUG-006-TEST-1",
      "test_success_criteria": [
        "After every ChatHealthy.ai Pipeline job completes, when no other automated work is taking place against ChatHealthyDataPipelines and no human has reserved the cluster, the cluster transitions to the PAUSED state within the operational tolerance defined for the cluster lifecycle manager.",
        "When other automated work IS taking place against the cluster, or a human reservation is held, the cluster does NOT pause."
      ],
      "discovery_date": "2026-05-06",
      "due_date": "2026-05-10",
      "reproduction": "1. Run any ChatHealthy.ai Pipeline job to completion (success, failure, or termination). 2. Confirm no other automated work is in Running / Pending state for the cluster, and no human reservation is held. 3. Observe the Atlas cluster ChatHealthyDataPipelines in the MongoDB Atlas console: the cluster remains in a non-PAUSED state.",
      "next_action": [
        "Investigate why the existing cluster-lifecycle implementation is not delivering the pause-when-idle behavior, and fix."
      ],
      "orphan": false
    }
  ]
}