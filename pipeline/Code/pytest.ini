[pytest]
markers =
    unit: pure-function tests, no I/O (LLD §9.2)
    integration: end-to-end pipeline slices (LLD §9.3)
    regression: collection interrogation per REQ table (LLD §9.4)

testpaths = tests
