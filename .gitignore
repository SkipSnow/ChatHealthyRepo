.env
*.env          # any name ENDING .env, not just a file named .env.
               # c<U+F03A>CHATHealthyLLC.env reached 5da7817d because
               # `.env` and `**/.env` match the NAME .env only.
code/.env
Code/.env
**/.env

# Managed deployment-fact files (Deployment Architecture v14 Ch 3.2 /
# 3.7). Bytes live in deployment_architecture.json embedded_content;
# Extractor materializes on demand, build deletes after use. Source
# tree MUST NOT hold these files.
pipeline/Code/requirements-pipeline.txt

SourcePropriataryData/

~*
*.tmp
*.bak
# timestamped backups: *.bak alone does not match .bak-20260808-143034,
# which is how four .env copies with live secrets reached the index.
*.bak-*

*.ipynb

.venv/
venv/
venv_func/
__pycache__/
*.pyc
*.egg-info/

.claude/backups/
.claude/file-history/
.claude/ide/
.claude/ChatHealthySettings.json
.claude/ChatHealthySettings.json.bak
.claude/plans/
.claude/sessions/
.claude/session-env/
.claude/shell-snapshots/
.claude/.credentials.json
.claude/mcp-needs-auth-cache.json
.claude/scheduled_tasks.lock

Code/ConversationalUX/FindCareChat/frontend/vite.config.ts

*.http

Code/DataPipelines/pipeline_files.json
Code/DataPipelines/pipeline_code_review.json

!.evv/
!.evv/**

node_modules/
dist/
Code/ConversationalUX/FindCareChat/backend/static/
.env.local

**/bin/
**/obj/
**/out/

*.log

brain/BusinessArtifacts/

Code/Shared/ops/certs/

/test_*.png
/umls_signup.png
_oneshots/test_output/

Code/Shared/ops/tools/caddy.exe

Analysis/*.mp4

**/build/
architecture/Conversation-logPipeline/bin/
architecture/Conversation-logPipeline/obj/
architecture/Conversation-logPipeline/out/
architecture/Conversation-logPipeline/build/
brain/machine_artifacts/.iteration_cache/

*.md

archive/

_oneshots/
**/_oneshots/

.wrangler/

architecture/DevOpsBuildDeployAndEnvironmentManagement/localBuild/
# Canonical build output root (operator directive 2026-08-04). Purged
# and rebuilt on every `build_chathealthy.py` invocation; `deploy_
# chathealthy.py` reads from here only.
/build/
.claude/worktrees/

architecture/EngineeringRuleEnforcement/ArchitectureDesignAndAuditDocs/commit_authorization.log
architecture/EngineeringRuleEnforcement/ArchitectureDesignAndAuditDocs/llm_classify_shell_invocation.cache.json

# Deploy staging. Derived from the authored sources by
# deploy_chathealthy.py, the same way build/ is. Nothing runs from
# here yet: the enforcement registry still points at the authored
# paths, so this tree is currently inert. If the registry is ever
# repointed at deploy/, whether this tree gets committed becomes a
# real question -- a fresh clone would need it to exist.
#
# Anchored. Unanchored, this matched every directory named deploy at
# any depth, including pipeline/deploy, which is authored source and
# holds the provider pipeline and Watchdog runbooks. The files already
# tracked there were safe, but a new one would have been invisible to
# git and would never have reached a build, which reads origin.
/deploy/

# Claude Code tool state — moved here from .git/info/exclude so the
# ignore list is in the repository and reviewable (2026-08-10).
.claude/scheduled_tasks.json
.claude/routines/.state/
.claude/checkpoints/
.claude/mailbox/
.claude/agent-registry.json
.claude/agent-memory-local
.claude/first-run
.claude/assistant-daemon-state.json
architecture/EngineeringRuleEnforcement/tests/_cold_start_seconds.txt
